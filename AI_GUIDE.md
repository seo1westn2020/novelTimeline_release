# AI 기능 가이드

> 📘 이 문서는 AI 기능(초고 생성·충돌 검증)을 처음 설정할 때 따라할 가이드입니다. 앱 업데이트 없이 갱신됩니다.

---

## 1. AI 기능이 하는 일

novel-timeline의 AI 기능은 두 가지로 나뉩니다.

### ✏️ AI 초고 생성
인물·사건·시간선이 입력된 상태에서 한 장면의 **본문 초안**을 자동으로 써 줍니다.

- 입력: 사건의 등장 인물·시점·요약·태그 + 인물의 성격·말투
- 출력: 약 1,500~5,000자 분량의 한 장면 초고 (선택한 언어로 응답)
- 용도: "처음 한 줄을 못 쓰겠다" "끊긴 흐름을 이어 쓰고 싶다" 같은 막힘 해소

### 🛡️ AI 충돌 검증
인물 성격·시간선·세계관의 **일관성 위반**을 점검합니다.

- 점검 항목 예시:
  - 인물의 **금기어**(설정상 절대 안 쓸 단어)가 등장하는지
  - 인물의 **말투 패턴**과 본문이 어긋나는지
  - 사건 시간 순서와 인물의 상태(생존/사망/실종)가 모순되지 않는지
  - 세력 소속이 그 시점과 일치하는지
- 출력: "충돌 없음" 또는 항목별 경고 카드

본문에 **이름만 적힌 인물**(태그하지 않은 인물)도 자동 인식하여 검증에 포함됩니다 (별칭/aliases 활용).

---

## 2. BYOK 정책 — Bring Your Own Key

novel-timeline의 AI는 **사용자 본인의 API 키**로 동작합니다.

- 키 발급: 사용자가 OpenAI / Anthropic / Google 중 한 곳에서 직접 발급
- 비용 청구: **사용량만큼** 해당 회사가 사용자에게 직접 청구 (novel-timeline은 결제 중계 없음)
- 데이터: novel-timeline 서버는 키도, 원고도 보관·중계하지 않습니다 — 앱이 직접 해당 회사 서버로 호출
- 로컬 옵션: **Ollama**를 쓰면 PC 안에서만 처리 → 비용 0, 외부 전송 0

> 💡 "구독한 ChatGPT Plus($20/월)와 API 키는 별개입니다." API는 따로 결제·충전이 필요합니다.

---

## 3. 4 프로바이더 비교 (실제 검수 결과 기반)

아래 표는 **동일 NTZ 샘플로 4 프로바이더를 실제 검수**한 결과를 반영한 것입니다. 자료실의 "frontier 모델일수록 좋다"는 일반 평판과 다른 결과가 나온 항목도 있어, 결과를 솔직하게 기록했습니다.

| 프로바이더 / 모델 | 충돌 검출 (동일 샘플) | 비용 (추정) | 키 발급 / 설치 | 가이드 |
|---|---|---|---|---|
| **Gemini 2.5 Flash** (Google, BYOK) | **5건** — 양자 아바타 vs 유전자 치료 같은 **개념 충돌**까지 잡음 | $0~0.005 / 1회 (무료 티어 有) | 쉬움 | [→ AI_GUIDE_GOOGLE.md](./AI_GUIDE_GOOGLE.md) |
| **GPT-4o** (OpenAI, BYOK) | **2건** — 균형 잡힌 안정 동작 | $0.005~0.02 / 1회 | 쉬움 | [→ AI_GUIDE_OPENAI.md](./AI_GUIDE_OPENAI.md) |
| **Ollama qwen2.5:14b** (로컬) | **2건 — GPT-4o급 검출** | $0 (PC 자원만, VRAM 10~12 GB 권장) | 중간 (설치 + 모델 9 GB 다운로드) | [→ AI_GUIDE_OLLAMA.md](./AI_GUIDE_OLLAMA.md) |
| **Ollama qwen2.5:7b** (로컬) | **2건 — 검출 약함** (놓치는 항목 多) | $0 (PC 자원만, VRAM 6 GB) | 중간 (설치 + 모델 4.7 GB 다운로드) | [→ AI_GUIDE_OLLAMA.md](./AI_GUIDE_OLLAMA.md) |
| **Claude (Anthropic)** | 검수 못 함 — ⚠️ 한국 결제 이슈 (아래 박스) | $0.005~0.02 / 1회 | 쉬움 (단, **결제 충전 막힘**) | [→ AI_GUIDE_ANTHROPIC.md](./AI_GUIDE_ANTHROPIC.md) |

> ⚠️ **Anthropic (Claude)는 현재 권장 외**: 2026-05 기준 한국 사용자가 API 크레딧 충전 시 **결제 버튼이 비활성화되는 사례**가 다수 보고됨. 다른 Anthropic 아이디로 시도해도 동일 증상. **Anthropic Console 외부 이슈**이며 사용자 환경 문제가 아닙니다. 충전이 풀릴 때까지는 위 3개 프로바이더(Gemini / OpenAI / Ollama) 사용을 권장합니다. 자세한 사정은 [AI_GUIDE_ANTHROPIC.md](./AI_GUIDE_ANTHROPIC.md) 참조.

**상황별 추천 (검수 결과 기반)**:
- **가장 정밀한 충돌 검출** → **Gemini 2.5 Flash** (실제 5건 검출, BYOK이지만 저렴·무료 티어)
- **무료·오프라인 최선** → **Ollama `qwen2.5:14b`** (사양 OK시. GPT-4o급 검출, 외부 전송 0)
- **저사양 PC라 14b가 안 돌아간다** → **Ollama `qwen2.5:7b`** (가볍지만 검출은 약함)
- **균형 잡힌 BYOK 선택** → **GPT-4o** (중간 비용, 안정 동작)
- **장면 한 편 길게 쓰고 싶다 + 결제 가능하다면** → Claude (현재 결제 이슈 해소된 사용자 한정)

---

## 4. 공통 사용 흐름

어느 프로바이더를 쓰든 흐름은 같습니다.

1. **키 발급 또는 Ollama 설치** — 위 표의 가이드 문서를 따라 진행
2. **novel-timeline 설정** — 햄버거 ≡ → 설정 → AI 설정에서 프로바이더·키 입력
3. **연결 테스트** — "연결 테스트" 버튼으로 200 응답 확인 (아래 5번)
4. **첫 사용** — 충돌 검사(아래 6번) 또는 초고 생성(아래 7번)

각 프로바이더 가이드에서 1·2단계의 상세를, 본 문서에서 3·4단계의 공통 부분을 다룹니다.

---

## 5. 연결 테스트 (공통)

설정이 끝났다면 실제 호출이 되는지 한 번 확인해 봅니다.

1. 햄버거 메뉴 **≡** 클릭
2. **설정 → AI 설정** 진입
3. 프로바이더·모델·키 입력 후 **"연결 테스트"** 버튼 클릭
4. 잠시 후 ✅ 200 OK 메시지가 뜨면 정상

```
<!-- 📸 스크린샷: 햄버거 ≡ → 설정 화면. AI 프로바이더 드롭다운이 보이는 상태 -->
```

```
<!-- 📸 스크린샷: 연결 테스트 버튼 클릭 → "연결 성공" 토스트가 뜬 화면 -->
```

**테스트 실패 시 점검 순서**:
- 키를 정확히 복사·붙여넣기 했는지 (앞뒤 공백 주의)
- 결제·충전이 등록되어 있는지 (각 프로바이더 가이드 참조)
- 인터넷 연결 상태
- 베이스 URL을 임의로 바꾸지 않았는지 (특별한 이유가 없다면 기본값 유지)

---

## 6. 첫 충돌 검사 (공통)

인물 1명 이상, 사건 1개 이상이 입력된 상태에서 시도합니다.

1. **사건 카드** 우상단의 🛡️ (방패) 아이콘 클릭
2. 잠시 후 우측 패널에 **결과 카드**가 표시됩니다
3. "충돌 없음"이면 정상 — 그 사건의 본문은 설정과 어긋남이 없다는 뜻
4. 항목별 경고가 뜨면 본문 또는 설정을 수정하면 됩니다

```
<!-- 📸 스크린샷: 사건 카드의 🛡️ 버튼 위치 + 결과 카드 -->
```

**잘 잡히는 패턴**:
- 인물 A의 금기어(예: "허세")가 본문에 등장
- 인물의 사망 시점 이후 사건에서 그 인물이 활동
- 시간순으로 보면 만났을 리 없는 두 인물의 대화

---

## 7. 첫 초고 생성 (공통)

빈 본문을 채우거나, 흐름이 막혔을 때 사용합니다.

1. **사건 카드** 우상단의 ✏️ (연필) 아이콘 클릭
2. 잠시 후 우측 패널에 **초고**가 생성됩니다 (등장 인물·시점·요약·태그 + 인물 성격·말투 기반)
3. 마음에 드는 부분만 골라 **챕터 본문에 붙여넣기**
4. 다시 다른 톤으로 받고 싶으면 한 번 더 ✏️ 클릭

```
<!-- 📸 스크린샷: 사건 카드의 ✏️ 버튼 + 생성된 초고 결과 -->
```

> 💡 초고는 **출발점**입니다. 그대로 쓰는 게 아니라, 막힌 흐름을 풀거나 첫 한 줄을 얻기 위한 도구로 사용하세요.

---

## 8. 자주 묻는 질문

### 비용이 청구되지 않는 모델은 없나요?
- **Gemini**의 일부 모델(예: `gemini-1.5-flash`)은 일정 사용량 이하 무료 티어를 제공합니다 (한도 초과 시 결제). [→ AI_GUIDE_GOOGLE.md](./AI_GUIDE_GOOGLE.md)
- **Ollama**(로컬)는 PC에서 직접 돌리므로 항상 무료입니다 (전기료·PC 자원만 사용). [→ AI_GUIDE_OLLAMA.md](./AI_GUIDE_OLLAMA.md)

### 원고 데이터가 외부로 전송되나요?
- OpenAI / Anthropic / Google 사용 시 — 해당 회사 서버로 호출 시점에 전송됩니다. 보관·학습 정책은 각 회사 약관에 따릅니다 (대부분 API 호출 데이터는 학습에 쓰지 않음을 명시).
- **Ollama**(로컬) — 원고가 PC 밖으로 나가지 않습니다.

### ChatGPT Plus 구독이 있는데 그걸로 안 되나요?
- ChatGPT 구독($20/월)과 OpenAI **API**는 별개의 결제입니다. API 키를 별도 발급하고 결제도 따로 등록해야 합니다. [→ AI_GUIDE_OPENAI.md](./AI_GUIDE_OPENAI.md)

### 한국어로 답이 잘 나오나요?
- 모든 프로바이더가 한국어 출력을 지원합니다. AI 응답 언어는 앱의 **표시 언어 설정**에 자동으로 맞춰집니다 (햄버거 ≡ → 언어).
- 일본어·중국어 출력도 같은 방식으로 동작합니다.

### 키를 잃어버렸어요
- 보통 **재발급**이 정답입니다. 옛 키는 폐기(revoke)하고 새 키를 발급해 앱에 다시 입력하면 됩니다. 각 프로바이더 가이드의 "키 발급" 섹션 참조.

---

> 추가 문의: **askNovelTimeline@gmail.com** / GitHub Issues
