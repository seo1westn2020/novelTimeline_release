# AI 가이드 — ChatGPT (OpenAI)

> 📘 OpenAI(ChatGPT) API 키 발급부터 novel-timeline 앱 설정까지 단계별 안내. 비개발자 작가 분들이 따라할 수 있도록 작성했습니다.
>
> 메인 가이드: [AI_GUIDE.md](./AI_GUIDE.md) (4 프로바이더 비교·공통 흐름)

---

## 0. 들어가기 전

- **결제는 한 번만**: 카드 등록 + $5~ 정도 충전이면 충돌 검사 약 1,000회 분량입니다.
- **ChatGPT Plus 구독($20/월)과는 별개**: 구독이 있어도 API는 따로 키 발급·결제가 필요합니다.
- 작업 시간 약 **15분** 예상 (계정 생성 ~ 첫 호출까지).

> 💡 **실제 검수 메모 (2026-05)**: 동일 NTZ 샘플로 4 프로바이더를 비교한 결과, **GPT-4o**는 충돌 **2건 검출**으로 안정적이고 균형 잡힌 동작을 보여 줍니다. 무난한 BYOK 선택지로 권장합니다. 단, 최정밀 검출을 원하시면 **Gemini 2.5 Flash**(5건 검출, 저렴) 쪽이 더 강한 결과를 보였습니다 — 4 프로바이더 비교는 [AI_GUIDE.md § 3](./AI_GUIDE.md#3-4-프로바이더-비교-실제-검수-결과-기반) 참조.

---

## 1. OpenAI 계정 만들기

1. 브라우저로 [platform.openai.com/signup](https://platform.openai.com/signup) 접속
2. 이메일·비밀번호 입력 → 가입 완료
3. **전화번호 인증** (SMS 코드)
4. 이름·소속(혼자라면 "Personal" 선택)을 묻는 화면을 통과하면 **Dashboard** 진입

```
<!-- 📸 스크린샷: OpenAI Dashboard 첫 화면 -->
```

> 💡 이미 ChatGPT 웹 계정이 있다면 같은 이메일로 platform.openai.com에 로그인하면 됩니다.

---

## 2. API 키 발급

1. Dashboard 왼쪽 사이드바 → **API keys** 메뉴
2. 우상단 **Create new secret key** 버튼
3. 이름은 식별할 수 있게 — 예: `novel-timeline`
4. **Create secret key** 클릭 → 키가 표시됩니다 (`sk-...` 형식)
5. 즉시 **Copy** 버튼으로 복사 → 메모장 등 안전한 곳에 붙여넣기 저장

```
<!-- 📸 스크린샷: OpenAI Dashboard → API keys 페이지의 Create 버튼 -->
```

```
<!-- 📸 스크린샷: 발급된 키가 한 번 표시되는 화면 + Copy 버튼 -->
```

> ⚠️ **이 키는 처음 한 번만 표시됩니다.** 창을 닫으면 다시 볼 수 없으니 반드시 즉시 복사·저장하세요. 잃어버리면 새 키를 발급받아야 합니다 (옛 키는 안전을 위해 Revoke).

---

## 3. 결제 등록

API는 **선결제(Prepaid)** 방식입니다. 카드 등록 + 일정 금액 충전 후, 사용량이 그 잔액에서 차감됩니다.

1. Dashboard → 좌측 사이드바 → **Billing** → **Payment methods**
2. **Add payment method** 클릭 → 카드 정보 입력
3. **Add to credit balance** → **$5** (최소 권장 충전액) → 결제
4. 충전 잔액이 표시되면 완료

```
<!-- 📸 스크린샷: OpenAI Billing → Payment methods + Add credit balance -->
```

> ⚠️ **결제를 등록하지 않으면 키가 발급돼도 호출 시 "Insufficient quota" 에러**가 발생합니다. 가장 흔한 막힘 지점입니다.

> 💡 ChatGPT 구독($20/월)과 API 충전은 **별도의 결제**입니다. 구독이 있어도 API 잔액은 0에서 시작합니다.

---

## 4. 사용량 한도 설정 (선택, 권장)

예상 외 호출이 많아도 카드가 무한정 빠져나가지 않도록 **월별 한도**를 설정해 둡니다.

1. Dashboard → **Limits** 메뉴
2. **Monthly budget** — 예: `$5` 입력 → 저장
3. 설정한 금액에 도달하면 자동으로 호출이 차단됩니다 (다음 달 자동 리셋)

```
<!-- 📸 스크린샷: OpenAI Limits → Monthly budget 설정 화면 -->
```

> 💡 처음에는 **$5** 정도가 안전합니다. 익숙해진 뒤 늘리면 됩니다.

---

## 5. novel-timeline 앱 설정

1. novel-timeline 실행
2. 우상단 **햄버거 메뉴 ≡** 클릭
3. **설정 → AI 설정** 진입
4. 다음 항목을 입력:

| 항목 | 값 |
|---|---|
| **프로바이더** | `OpenAI` |
| **베이스 URL** | 기본값 `https://api.openai.com` 그대로 (변경 금지) |
| **API 키** | 2단계에서 발급받은 키 (`sk-...`) 붙여넣기 |
| **모델** | `gpt-4o-mini` 권장 (가성비) 또는 `gpt-4o` (품질 우선) |

5. **연결 테스트** 버튼 클릭 → ✅ "연결 성공" 메시지 확인
6. 설정 화면 닫기

```
<!-- 📸 스크린샷: novel-timeline 설정 → AI 프로바이더 드롭다운에서 OpenAI 선택 + 키 입력 칸 + 모델 드롭다운 -->
```

```
<!-- 📸 스크린샷: 연결 테스트 버튼 → 연결 성공 토스트 -->
```

> 💡 **모델 추천**:
> - `gpt-4o-mini` — 충돌 검사·간단 초고에 충분, 비용 1/10 수준
> - `gpt-4o` — 긴 장면 초고·세밀한 한국어 표현이 필요할 때
>
> 처음에는 `gpt-4o-mini`로 시작하고, 결과가 부족하다 싶을 때 `gpt-4o`로 올리는 것을 권장합니다.

---

## 6. 첫 충돌 검사 / 초고 생성

설정이 끝났다면 메인 가이드의 공통 절차를 따릅니다.

- **첫 충돌 검사**: [AI_GUIDE.md § 6](./AI_GUIDE.md#6-첫-충돌-검사-공통)
- **첫 초고 생성**: [AI_GUIDE.md § 7](./AI_GUIDE.md#7-첫-초고-생성-공통)

---

## 7. 비용 추정

`gpt-4o-mini` 기준 (2026년 기준 — 가격은 OpenAI 공식 페이지 우선):

| 작업 | 1회당 추정 비용 | $5 충전 시 가능 횟수 |
|---|---|---|
| 충돌 검사 | 약 **$0.005** | 약 **1,000회** |
| 초고 생성 (1장면, 2~5천자) | 약 **$0.01** | 약 **500회** |

`gpt-4o` 사용 시 위 금액의 약 **5~10배**가 됩니다 (긴 장면일수록 차이 커짐).

> 💡 사용량은 OpenAI Dashboard → **Usage** 메뉴에서 실시간 확인 가능.

---

## 8. 자주 묻는 문제

### "401 Unauthorized" 에러
- **원인**: 키가 잘못 입력되었거나, 옛 키가 폐기됨
- **해결**: 키를 다시 복사해 붙여넣기. 그래도 안 되면 OpenAI Dashboard → API keys에서 **새 키 발급** 후 앱에 다시 입력

### "429 Rate limit" 에러
- **원인**: 짧은 시간에 너무 많이 호출
- **해결**: 1~2분 기다린 뒤 재시도. 또는 OpenAI Dashboard → Limits에서 RPM 한도 확인

### "Insufficient quota" 에러
- **원인**: 결제가 등록되지 않았거나, 충전 잔액이 0
- **해결**: 위 **3단계 결제 등록** 완료. 이미 했다면 Dashboard → Billing에서 잔액 확인

### "연결 테스트는 되는데 충돌 검사·초고에서 실패"
- **원인**: 모델명이 계정 권한과 안 맞을 수 있음 (특히 `gpt-4o`는 일부 신규 계정에서 별도 활성화 필요)
- **해결**: 모델을 `gpt-4o-mini`로 바꾸고 다시 시도

### 키를 실수로 공개해 버렸어요
- 즉시 OpenAI Dashboard → API keys → 해당 키의 **Revoke** 클릭 → 새 키 발급
- 누구든 노출된 키로 호출하면 **사용자 계정의 잔액**이 차감되므로, **즉시** Revoke하는 것이 중요

---

## 다음 단계

→ 메인 가이드로 돌아가 **첫 충돌 검사**를 시도해 보세요: [AI_GUIDE.md § 6](./AI_GUIDE.md#6-첫-충돌-검사-공통)

---

> 추가 문의: **askNovelTimeline@gmail.com** / GitHub Issues
